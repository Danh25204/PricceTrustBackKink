---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/css/base.css
  - assets/css/layout.css
  - assets/css/components.css
  - assets/css/animations.css
  - assets/js/scene.js
  - assets/js/animations.js
  - assets/js/ui.js
  - assets/js/main.js
  - assets/js/vendor/.gitkeep
  - assets/images/.gitkeep
  - assets/fonts/.gitkeep
  - index.html
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-02
  - DESIGN-03
  - DESIGN-04
  - DESIGN-05
  - DESIGN-06
  - PERF-07
  - PERF-08

must_haves:
  truths:
    - "CSS custom properties for the gold-black palette are declared in :root and resolvable in DevTools"
    - "Playfair Display and Be Vietnam Pro fonts load via Google Fonts CSS2 API with display=swap"
    - "All folder names and filenames are lowercase"
    - "All asset paths in index.html use ./assets/ relative format"
    - "Featured card and non-featured card border/shadow tokens are declared and named exactly per REQUIREMENTS.md"
  artifacts:
    - path: "assets/css/base.css"
      provides: "All CSS custom properties — colors, spacing, typography, z-index, card tokens, reset, reduced-motion"
      contains: ":root {"
    - path: "index.html"
      provides: "Minimal HTML5 scaffold with Google Fonts <link> and base.css <link>"
      contains: "./assets/css/base.css"
    - path: "assets/js/vendor/.gitkeep"
      provides: "Tracks otherwise-empty vendor directory in git"
    - path: "assets/images/.gitkeep"
      provides: "Tracks otherwise-empty images directory in git"
    - path: "assets/fonts/.gitkeep"
      provides: "Tracks otherwise-empty fonts directory in git"
  key_links:
    - from: "index.html"
      to: "assets/css/base.css"
      via: "<link rel=\"stylesheet\" href=\"./assets/css/base.css\">"
      pattern: "href=\"./assets/css/base\\.css\""
    - from: "index.html"
      to: "fonts.googleapis.com"
      via: "<link rel=\"stylesheet\" href=\"https://fonts.googleapis.com/css2?...\">"
      pattern: "fonts\\.googleapis\\.com/css2"
---

<objective>
Create the CSS design token file (base.css), the complete folder structure, and a minimal index.html scaffold that loads the tokens and fonts. Every token name established here becomes a load-bearing contract for Phase 3 and beyond — no changes to token names after this plan is merged.

Purpose: Phase 2 (HTML Skeleton) and Phase 3 (CSS Styling) depend entirely on these token names being correct, stable, and resolvable. Getting a token name wrong here means a codebase-wide find-and-replace later.

Output: `assets/css/base.css` with all design tokens; `index.html` with Google Fonts + base.css wired in `<head>`; empty placeholder files in vendor/, images/, fonts/ so Git tracks the directories.
</objective>

<execution_context>
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/PROJECT.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/ROADMAP.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/REQUIREMENTS.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/phases/01-foundation/01-RESEARCH.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/phases/01-foundation/01-UI-SPEC.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/CLAUDE.md

<interfaces>
<!-- Authoritative token names from REQUIREMENTS.md DESIGN-01 through DESIGN-06 -->
<!-- These names are the final contract — downstream phases reference them as var(--name) -->

DESIGN-01 required tokens:
  --gold-bright: #FFD700
  --gold-mid:    #C9A227
  --black-rich:  #0A0A0A
  --black-card:  #111111
  --text-primary: #F5F0E8

DESIGN-02 required token:
  --overlay-dark: rgba(0, 0, 0, 0.65)

DESIGN-05 required tokens:
  --card-featured-border: 1px solid var(--gold-bright)
  --card-featured-shadow: 0 0 24px var(--gold-glow)

DESIGN-06 required token:
  --card-default-border: 1px solid var(--black-border)

Extended tokens from UI-SPEC.md (must match these names exactly):
  --gold-dim:          #8B6914
  --black-border:      #1E1E1E
  --text-muted:        #8A8070
  --gold-glow:         rgba(255, 215, 0, 0.35)
  --gold-glow-dim:     rgba(255, 215, 0, 0.15)
  --color-error:       #FF4444
  --space-xs: 4px  --space-sm: 8px  --space-md: 16px  --space-lg: 24px
  --space-xl: 32px  --space-2xl: 48px  --space-3xl: 64px  --cta-touch: 56px
  --text-sm: 14px  --text-base: 16px  --text-lg: 28px  --text-xl: 40px
  --z-canvas: 0  --z-overlay: 1  --z-content: 2  --z-float: 100  --z-modal: 200
  --card-bg: linear-gradient(135deg, var(--black-card) 0%, #0D0D0D 100%)
  --card-hover-lift: translateY(-4px)
  --bp-mobile: 768px
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create base.css with all design tokens</name>
  <files>assets/css/base.css</files>
  <read_first>
    - C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/REQUIREMENTS.md (DESIGN-01 through DESIGN-06 exact values)
    - C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/phases/01-foundation/01-UI-SPEC.md (Color, Typography, Spacing, Z-Index, Featured Card Token Contract sections)
    - C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/phases/01-foundation/01-RESEARCH.md (Pattern 1: CSS Custom Property Declaration Structure; Pattern 4: CSS Reset Block; complete base.css template in Code Examples section)
    - C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/CLAUDE.md (Colors section, Performance section)
  </read_first>
  <action>
    Create `assets/css/base.css` (create the `assets/css/` directory if it does not exist). The file must contain exactly six `:root` blocks in this order, using the names from the <interfaces> block above (no variations, no aliases):

    Block 1 — COLORS: declare `--gold-bright: #FFD700`, `--gold-mid: #C9A227`, `--black-rich: #0A0A0A`, `--black-card: #111111`, `--text-primary: #F5F0E8`, `--gold-dim: #8B6914`, `--black-border: #1E1E1E`, `--text-muted: #8A8070`, `--overlay-dark: rgba(0, 0, 0, 0.65)`, `--gold-glow: rgba(255, 215, 0, 0.35)`, `--gold-glow-dim: rgba(255, 215, 0, 0.15)`, `--color-error: #FF4444`.

    Block 2 — SPACING: `--space-xs: 4px`, `--space-sm: 8px`, `--space-md: 16px`, `--space-lg: 24px`, `--space-xl: 32px`, `--space-2xl: 48px`, `--space-3xl: 64px`, `--cta-touch: 56px`.

    Block 3 — TYPOGRAPHY: `--text-sm: 14px`, `--text-base: 16px`, `--text-lg: 28px`, `--text-xl: 40px`.

    Block 4 — Z-INDEX STACK: `--z-canvas: 0`, `--z-overlay: 1`, `--z-content: 2`, `--z-float: 100`, `--z-modal: 200`.

    Block 5 — CARD TOKENS: `--card-featured-border: 1px solid var(--gold-bright)`, `--card-featured-shadow: 0 0 24px var(--gold-glow)`, `--card-default-border: 1px solid var(--black-border)`, `--card-bg: linear-gradient(135deg, var(--black-card) 0%, #0D0D0D 100%)`, `--card-hover-lift: translateY(-4px)`.

    Block 6 — BREAKPOINTS: `--bp-mobile: 768px`. Add a comment that this value is for documentation only; do NOT use `var(--bp-mobile)` inside media query conditions — media queries must use the literal value `768px`.

    After all :root blocks, append:
    - A `*, *::before, *::after` box-sizing reset with `margin: 0; padding: 0`.
    - An `html` rule: `font-size: 16px; -webkit-text-size-adjust: 100%`.
    - A `body` rule: `background-color: var(--black-rich); color: var(--text-primary); font-family: 'Be Vietnam Pro', system-ui, -apple-system, sans-serif; font-size: var(--text-base); line-height: 1.6`.
    - A `@media (prefers-reduced-motion: reduce)` block setting `animation-duration: 0.01ms !important` and `transition-duration: 0.01ms !important` on `*, *::before, *::after`.

    Also create these four empty placeholder CSS files (no content, 0 bytes each — Phase 3 fills them):
    - `assets/css/layout.css`
    - `assets/css/components.css`
    - `assets/css/animations.css`
  </action>
  <verify>
    <automated>grep -c "\-\-gold-bright: #FFD700" "C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/base.css"</automated>
  </verify>
  <acceptance_criteria>
    - `assets/css/base.css` exists
    - `assets/css/base.css` contains `:root {`
    - `assets/css/base.css` contains `--gold-bright: #FFD700`
    - `assets/css/base.css` contains `--gold-mid:    #C9A227` (or `--gold-mid: #C9A227`)
    - `assets/css/base.css` contains `--black-rich:  #0A0A0A` (or `--black-rich: #0A0A0A`)
    - `assets/css/base.css` contains `--black-card:  #111111` (or `--black-card: #111111`)
    - `assets/css/base.css` contains `--text-primary: #F5F0E8`
    - `assets/css/base.css` contains `--overlay-dark: rgba(0, 0, 0, 0.65)`
    - `assets/css/base.css` contains `--gold-glow:     rgba(255, 215, 0, 0.35)` (or `--gold-glow: rgba(255, 215, 0, 0.35)`)
    - `assets/css/base.css` contains `--card-featured-border: 1px solid var(--gold-bright)`
    - `assets/css/base.css` contains `--card-featured-shadow: 0 0 24px var(--gold-glow)`
    - `assets/css/base.css` contains `--card-default-border:  1px solid var(--black-border)` (or `--card-default-border: 1px solid var(--black-border)`)
    - `assets/css/base.css` contains `--black-border: #1E1E1E`
    - `assets/css/base.css` contains `--z-canvas:  0` (or `--z-canvas: 0`)
    - `assets/css/base.css` contains `--z-float:   100` (or `--z-float: 100`)
    - `assets/css/base.css` contains `box-sizing: border-box`
    - `assets/css/base.css` contains `background-color: var(--black-rich)`
    - `assets/css/base.css` contains `prefers-reduced-motion: reduce`
    - `assets/css/layout.css` exists (0 bytes or empty)
    - `assets/css/components.css` exists (0 bytes or empty)
    - `assets/css/animations.css` exists (0 bytes or empty)
    - No filename in `assets/css/` contains an uppercase letter
  </acceptance_criteria>
  <done>base.css exists containing all 12 color tokens, 8 spacing tokens, 4 typography tokens, 5 z-index tokens, 5 card tokens, 1 breakpoint reference, a box-sizing reset, a body rule, and a prefers-reduced-motion media query. Three empty CSS placeholder files exist alongside it.</done>
</task>

<task type="auto">
  <name>Task 2: Create minimal index.html scaffold and empty JS placeholder files</name>
  <files>
    index.html,
    assets/js/scene.js,
    assets/js/animations.js,
    assets/js/ui.js,
    assets/js/main.js,
    assets/js/vendor/.gitkeep,
    assets/images/.gitkeep,
    assets/fonts/.gitkeep
  </files>
  <read_first>
    - C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/phases/01-foundation/01-RESEARCH.md (Pattern 2: Google Fonts CSS2 Import; Pattern 3: Minimal index.html Scaffold; Anti-Patterns section — specifically the warning about root-relative paths and crossorigin attribute; Pitfall 5 about empty directories and .gitkeep)
    - C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/CLAUDE.md (Critical rules — filenames lowercase, asset paths relative)
  </read_first>
  <action>
    Create `index.html` at the repo root. It must be a valid HTML5 document with `lang="vi"`. The `<head>` must contain, in this exact order:
    1. `<meta charset="UTF-8">`
    2. `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
    3. `<title>TrustBackLink — Báo Giá Dịch Vụ SEO</title>`
    4. `<link rel="preconnect" href="https://fonts.googleapis.com">` (no crossorigin)
    5. `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>` (crossorigin required — fonts are cross-origin resources; omitting it means the preconnect hint does not warm the TLS connection used for font file downloads)
    6. `<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Be+Vietnam+Pro:wght@400;600&display=swap">` (this is the CSS2 API URL; &subset=vietnamese is not needed — the CSS2 API serves unicode-range blocks automatically for Vietnamese; do NOT use @import for fonts in CSS — that is render-blocking)
    7. `<link rel="stylesheet" href="./assets/css/base.css">` (relative path — do NOT use /assets/css/base.css — root-relative paths break on GitHub Pages sub-path deployments)

    The `<body>` should contain only an HTML comment: `<!-- Phase 2 adds all content here -->`.

    Also create the following JS placeholder files (single-line comment content only — no executable code):
    - `assets/js/scene.js` — content: `// Phase 4: Three.js gold particle scene — init() and resize() exported here`
    - `assets/js/animations.js` — content: `// Phase 5: GSAP ScrollTrigger entrance animations`
    - `assets/js/ui.js` — content: `// Phase 2: UI helpers (tab switching, etc.)`
    - `assets/js/main.js` — content: `// Phase 5: Entry point — wires GSAP ticker to Three.js renderer`

    Create these empty `.gitkeep` files to force Git to track the empty directories:
    - `assets/js/vendor/.gitkeep` (0 bytes — Three.js and GSAP vendor files land here in Phase 4/5)
    - `assets/images/.gitkeep` (0 bytes — logo.svg and og-image.jpg land here)
    - `assets/fonts/.gitkeep` (0 bytes — reserved for self-hosted font fallback if needed)
  </action>
  <verify>
    <automated>grep -c "href=\"./assets/css/base.css\"" "C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/index.html"</automated>
  </verify>
  <acceptance_criteria>
    - `index.html` exists at repo root
    - `index.html` contains `<!DOCTYPE html>`
    - `index.html` contains `<html lang="vi">`
    - `index.html` contains `<meta charset="UTF-8">`
    - `index.html` contains `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
    - `index.html` contains `<title>TrustBackLink — Báo Giá Dịch Vụ SEO</title>`
    - `index.html` contains `href="https://fonts.googleapis.com"` (preconnect)
    - `index.html` contains `href="https://fonts.gstatic.com" crossorigin` (preconnect with crossorigin)
    - `index.html` contains `fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Be+Vietnam+Pro:wght@400;600&display=swap`
    - `index.html` contains `href="./assets/css/base.css"` (relative path, not /assets/)
    - `index.html` does NOT contain `/assets/css/base.css"` starting with a forward slash
    - `assets/js/scene.js` exists
    - `assets/js/animations.js` exists
    - `assets/js/ui.js` exists
    - `assets/js/main.js` exists
    - `assets/js/vendor/.gitkeep` exists
    - `assets/images/.gitkeep` exists
    - `assets/fonts/.gitkeep` exists
    - No filename in `assets/js/` or `assets/images/` or `assets/fonts/` contains an uppercase letter
  </acceptance_criteria>
  <done>index.html exists at repo root with Google Fonts CSS2 preconnect + stylesheet link (with crossorigin on fonts.gstatic.com) and a relative ./assets/css/base.css link. Four JS placeholder files exist. Three .gitkeep files ensure Git tracks vendor/, images/, and fonts/ directories.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML head → Google Fonts CDN | Browser fetches stylesheet from fonts.googleapis.com; response is CSS (not JS) — low risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Google Fonts CDN (fonts.googleapis.com) | accept | CDN delivers CSS only (no JS execution); page is a static pricing display with no PII; third-party CSS risk is low for this use case |
| T-01-02 | Information Disclosure | index.html (public) | accept | Static public page — full disclosure is the intent; no secrets in Phase 1 artifacts |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Phase 1 installs zero packages — no package manager involved; no slopcheck gate needed |
</threat_model>

<verification>
After both tasks complete:

1. Open `index.html` in a browser. Open DevTools > Elements panel > select `:root` in the DOM. In the Computed tab, search for `--gold-bright`. It must show `#FFD700`.

2. Verify all five DESIGN-01 tokens resolve in DevTools `:root` computed styles: `--gold-bright`, `--gold-mid`, `--black-rich`, `--black-card`, `--text-primary`.

3. Verify `--overlay-dark` resolves to `rgba(0, 0, 0, 0.65)` (DESIGN-02).

4. Verify `--card-featured-border` and `--card-featured-shadow` and `--card-default-border` all resolve (DESIGN-05, DESIGN-06).

5. In DevTools Network tab, confirm `base.css` loads with status 200 and the path in the request URL is `./assets/css/base.css` (relative).

6. Shell check — no uppercase in filenames:
   `ls assets/css/ assets/js/` — all names must be lowercase (PERF-07).

7. Shell check — no root-relative paths in index.html:
   `grep -n "href\|src" index.html` — confirm no entry starts with `/assets/` (PERF-08).
</verification>

<success_criteria>
- `assets/css/base.css` exists with all 12 color tokens, 8 spacing tokens, 4 typography tokens, 5 z-index tokens, 5 card tokens, box-sizing reset, body rule, and prefers-reduced-motion media query
- `index.html` exists with Google Fonts CSS2 link (Playfair Display 700 + Be Vietnam Pro 400;600, display=swap) and relative `./assets/css/base.css` link
- All folder names and filenames are lowercase
- All asset paths in index.html use `./assets/` relative format (PERF-07, PERF-08)
- DESIGN-01 through DESIGN-06 tokens all resolve in browser DevTools without any CSS variable returning `initial` or transparent
- Empty directory structure committed via .gitkeep files: vendor/, images/, fonts/
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
