---
phase: 04-three-js-scene
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - index.html
  - assets/css/layout.css
autonomous: true
requirements:
  - HERO-03
  - HERO-04
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
  - PERF-05

must_haves:
  truths:
    - "A <canvas id='particle-canvas'> element exists in index.html inside the <body>, before </body>"
    - "canvas#particle-canvas has CSS: position fixed, z-index 0, pointer-events none, width 100vw, height 100vh, top 0, left 0"
    - "On mobile (<768px) canvas#particle-canvas is hidden via CSS (display none or visibility hidden)"
    - "On mobile (<768px) .hero has a CSS radial-gradient gold-black background — no JS required"
    - "On desktop, the canvas sits below the hero overlay (z-index 0 < --z-overlay 1 < --z-content 2)"
    - "index.html loads scene.js as type='module' and calls scene.init(canvas) + scene.resize() after DOMContentLoaded"
    - "If WebGL init fails, the canvas element is hidden and the CSS gradient fallback on .hero remains visible"
  artifacts:
    - path: "index.html"
      provides: "canvas element + scene.js module script tag"
      contains: "particle-canvas"
    - path: "assets/css/layout.css"
      provides: "canvas positioning rules + mobile fallback gradient"
      contains: "#particle-canvas"
  key_links:
    - from: "index.html <script type='module'>"
      to: "assets/js/scene.js init()"
      via: "import { init, resize, render } from './assets/js/scene.js'"
      pattern: "scene.js"
    - from: "#particle-canvas CSS"
      to: "z-index stack"
      via: "z-index: var(--z-canvas) which equals 0"
      pattern: "z-canvas"
    - from: ".hero mobile fallback"
      to: "radial-gradient"
      via: "@media (max-width: 768px) .hero background override"
      pattern: "radial-gradient"
---

<objective>
Wire the scene.js module into index.html and add the necessary CSS: inject the canvas element, position it (fixed, z-index 0, pointer-events none), add the mobile CSS radial-gradient fallback on .hero, hide the canvas on mobile, and add the script tag that calls init() after DOMContentLoaded.

Purpose: Completes Phase 4 — the particle scene is now visible in the browser. Phase 5 (GSAP) will replace the inline render call in main.js with gsap.ticker.add(scene.render), but that wiring is Phase 5's job. Phase 4 uses a temporary self-contained DOMContentLoaded bootstrap in the script tag so the scene is immediately testable.

Output: Updated index.html (canvas + script tag) and updated assets/css/layout.css (canvas CSS + mobile fallback).
</objective>

<execution_context>
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/PROJECT.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/ROADMAP.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/STATE.md

<interfaces>
<!-- From 04-01-PLAN.md — scene.js public API -->
Exported from assets/js/scene.js:
  init(canvasEl: HTMLCanvasElement): void
    — guards on mobile < 768 and no-WebGL; safe to call unconditionally
  resize(): void
    — safe to call before init (returns early if renderer not set up)
  render(): void
    — safe to call before init (returns early if needsRender false or renderer null)

Canvas element spec:
  id: "particle-canvas"
  No width/height HTML attributes — size set by renderer.setSize() in JS

Z-index stack (base.css — do not change these tokens):
  --z-canvas:  0   (canvas sits here)
  --z-overlay: 1   (hero::before dark overlay)
  --z-content: 2   (all text content)
  --z-float:   100 (floating CTA button)

Mobile fallback radial-gradient spec:
  background: radial-gradient(ellipse at 50% 30%, rgba(201,162,39,0.25) 0%, rgba(10,10,10,1) 60%)
  Applied on .hero at max-width: 768px (literal value — CSS vars not valid in @media)
  This gradient is always applied on mobile; it shows through when canvas is hidden

layout.css existing hero rules (read before editing — do NOT remove):
  .hero { min-height: 60vh; position: relative; background: var(--black-rich); ... }
  .hero::before { content:''; position:absolute; inset:0; background:var(--overlay-dark); z-index:var(--z-overlay); pointer-events:none; }
  .hero > * { position: relative; z-index: var(--z-content); }

index.html structure (relevant parts):
  <body>
    <header class="site-header">...</header>
    <section id="hero" class="hero">...</section>
    <main>...</main>
    <footer class="site-footer">...</footer>
    <a id="cta-float">...</a>
    <!-- canvas and script go here, before </body> -->
  </body>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canvas element and scene.js script tag to index.html</name>
  <files>index.html</files>
  <read_first>
    - index.html (full file — identify exact position of closing </body> tag for canvas + script insertion)
    - assets/js/scene.js (confirm exports: init, resize, render)
    - CLAUDE.md (key rule: all paths relative ./assets/; no root-relative paths)
  </read_first>
  <action>
    Insert two additions into index.html immediately before the closing </body> tag (after the #cta-float anchor):

    1. Canvas element:
       Add: <canvas id="particle-canvas" aria-hidden="true"></canvas>
       No width or height attributes — JS controls size via renderer.setSize().
       aria-hidden="true" because it is a decorative background, invisible to screen readers.

    2. Inline module script — Phase 4 bootstrap (will be replaced/extended by Phase 5 main.js):
       Add a <script type="module"> block with these exact responsibilities:
         a. Import init, resize, render from './assets/js/scene.js'
         b. Wait for DOMContentLoaded (or run immediately if already fired)
         c. Grab: const canvas = document.getElementById('particle-canvas')
         d. Call: init(canvas)
         e. Call: resize()
         f. Attach window resize listener: window.addEventListener('resize', resize)
         g. Start a minimal render loop ONLY as a Phase 4 temporary fallback — use requestAnimationFrame here (in index.html, NOT in scene.js) to call render() each frame. Comment clearly: "TODO Phase 5: remove this rAF loop and replace with gsap.ticker.add(render)"
         h. This rAF loop is acceptable in index.html because it is the orchestrator file, not scene.js. The constraint is that scene.js itself must not call rAF.

    Do NOT touch any existing content in index.html. Only append before </body>.
    Do NOT add a <link> to a new CSS file — canvas CSS goes into layout.css (Task 2 of this plan).
  </action>
  <verify>
    <automated>grep -c "particle-canvas" index.html</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "particle-canvas" index.html returns >= 2 (one in canvas element, one in getElementById call)
    - grep -c 'type="module"' index.html returns >= 1
    - grep -c "scene.js" index.html returns >= 1
    - grep "aria-hidden" index.html contains 'aria-hidden="true"' on the canvas element
    - grep -c "TODO Phase 5" index.html returns >= 1 (confirms the rAF is marked for replacement)
    - grep -c "import.*scene.js" index.html returns >= 1
    - The canvas element appears before </body> (tail of index.html shows canvas before </body>)
  </acceptance_criteria>
  <done>index.html has a canvas#particle-canvas element and a type="module" script that imports scene.js, calls init(canvas) + resize(), and runs a temporary rAF render loop marked for Phase 5 replacement.</done>
</task>

<task type="auto">
  <name>Task 2: Add canvas CSS positioning and mobile fallback gradient to layout.css</name>
  <files>assets/css/layout.css</files>
  <read_first>
    - assets/css/layout.css (full file — append new rules at end, do not modify existing .hero rules)
    - assets/css/base.css (confirm z-index token names: --z-canvas, --z-overlay, --z-content)
    - CLAUDE.md (all filenames lowercase, paths relative)
  </read_first>
  <action>
    Append a new section to the END of assets/css/layout.css. Do not modify any existing rules.

    Section heading comment: "SECTION 10: THREE.JS CANVAS — PERF-01, HERO-03, HERO-04"

    Rule 1 — Canvas positioning (PERF-01):
      #particle-canvas {
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        z-index: var(--z-canvas);
        pointer-events: none;
        display: block;
      }

    Rule 2 — Mobile canvas hide + hero gradient fallback (HERO-04):
      @media (max-width: 768px) {
        #particle-canvas {
          display: none;
        }
        .hero {
          background: radial-gradient(ellipse at 50% 30%, rgba(201, 162, 39, 0.25) 0%, rgba(10, 10, 10, 1) 60%);
        }
      }

    Note on the gradient: rgba(201,162,39,0.25) is --gold-mid (#C9A227) at 25% opacity; rgba(10,10,10,1) is --black-rich (#0A0A0A). Hardcoded in CSS because CSS variables are not reliably supported inside gradients on older Android WebViews.

    Note on z-index layering verification:
      canvas z-index 0 (--z-canvas) sits behind hero::before z-index 1 (--z-overlay) which sits behind text z-index 2 (--z-content). This is already correct in existing layout.css — do NOT change existing z-index values.

    WebGL unavailable fallback: no additional CSS needed. When init() returns early (no WebGL), the canvas element remains in DOM but is visually empty/transparent (alpha: true renderer background). The hero::before dark overlay and page background-color: var(--black-rich) on body ensure no black rectangle appears. To make this bulletproof, also add:
      #particle-canvas.hidden {
        display: none;
      }
    Phase 4 bootstrap script can add class 'hidden' to the canvas if WebGL detection in scene.js returns early — but since scene.js does not signal failure externally, the canvas will simply show nothing (transparent). This is acceptable for Phase 4; Phase 5 can add explicit error signalling if needed.
  </action>
  <verify>
    <automated>grep -c "particle-canvas" assets/css/layout.css</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "particle-canvas" assets/css/layout.css returns >= 2 (desktop rule + mobile rule)
    - grep -c "position: fixed" assets/css/layout.css returns >= 1 on the #particle-canvas rule
    - grep -c "pointer-events: none" assets/css/layout.css returns >= 1 on the #particle-canvas rule
    - grep -c "z-index: var(--z-canvas)" assets/css/layout.css returns 1
    - grep -c "display: none" assets/css/layout.css returns >= 1 inside the @media (max-width: 768px) block
    - grep -c "radial-gradient" assets/css/layout.css returns 1
    - grep -c "rgba(201" assets/css/layout.css returns >= 1 (gold-mid color in gradient)
    - Existing .hero, .hero::before, .hero > * rules are unchanged (grep confirms they still exist with original values)
  </acceptance_criteria>
  <done>layout.css has a SECTION 10 block: canvas is fixed/full-viewport/z-index 0/pointer-events none on desktop; canvas is display:none and .hero shows radial-gradient fallback on mobile <= 768px.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script type="module" in index.html | Runs client-side JS with DOM access; canvas is decorative, no user data |
| canvas element | Decorative background; aria-hidden prevents screen reader confusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Spoofing | canvas element (decorative) | accept | aria-hidden="true" declared; no input processing |
| T-04-02-02 | Denial of Service | rAF loop in index.html | accept | Temporary Phase 4 loop; replaced by GSAP ticker in Phase 5; demand-based inside scene.js limits actual GPU work |
| T-04-02-03 | Information Disclosure | Three.js CDN (esm.sh) loading in browser | accept | Static pricing page, no user credentials, no PII; CDN failure = no particles, graceful (page still functional) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan |
</threat_model>

<verification>
After execution, verify the complete plan:

1. grep -c "particle-canvas" index.html  → >= 2
2. grep -c 'type="module"' index.html  → >= 1
3. grep -c "scene.js" index.html  → >= 1
4. grep -c "particle-canvas" assets/css/layout.css  → >= 2
5. grep -c "position: fixed" assets/css/layout.css  → >= 1
6. grep -c "pointer-events: none" assets/css/layout.css  → >= 1
7. grep -c "radial-gradient" assets/css/layout.css  → 1
8. grep -c "display: none" assets/css/layout.css  → >= 1
9. Open index.html in a desktop browser: gold particles should float in the background of the hero section
10. Resize browser to < 768px: canvas disappears, hero shows a subtle gold glow gradient
</verification>

<success_criteria>
Phase 4 is complete when:
- canvas#particle-canvas is in index.html with aria-hidden="true"
- layout.css positions canvas as fixed/full-viewport/z-index 0/pointer-events none
- Mobile breakpoint hides canvas and shows radial-gradient fallback on .hero
- scene.js is imported as a module; init(canvas) + resize() called on DOMContentLoaded
- A temporary rAF loop calls scene.render() each frame (marked TODO Phase 5)
- Opening index.html in desktop browser shows gold floating particles behind hero content
- All existing layout.css rules are intact; no regressions to header, hero text, pricing cards
</success_criteria>

<output>
Create .planning/phases/04-three-js-scene/04-02-SUMMARY.md when done
</output>
