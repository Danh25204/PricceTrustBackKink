---
phase: 04-three-js-scene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/js/scene.js
autonomous: true
requirements:
  - HERO-03
  - HERO-04
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
  - PERF-05

must_haves:
  truths:
    - "scene.js exports init() and resize() — no other symbols on the public surface"
    - "init() returns early (no-op) when window.innerWidth < 768 — Three.js never initialises on mobile"
    - "init() returns early without error when WebGL is unavailable"
    - "On desktop (>=768px) 1000 gold particles are rendered; on mobile (<768px) init() returns early — no particles are initialized; mobile visual fallback is CSS radial-gradient only"
    - "renderer.setPixelRatio is called with Math.min(window.devicePixelRatio, 1.5)"
    - "No call to requestAnimationFrame anywhere in scene.js — rendering is demand-based via needsRender flag"
    - "scene.js has zero import of GSAP or animations.js — testable standalone"
  artifacts:
    - path: "assets/js/scene.js"
      provides: "Complete Three.js gold particle module"
      exports: ["init", "resize"]
      min_lines: 80
  key_links:
    - from: "assets/js/scene.js init()"
      to: "Three.js WebGLRenderer"
      via: "new THREE.WebGLRenderer({ canvas, alpha: true, antialias: false })"
      pattern: "WebGLRenderer"
    - from: "assets/js/scene.js"
      to: "needsRender flag"
      via: "window mousemove and resize events set needsRender = true"
      pattern: "needsRender"
---

<objective>
Write the complete assets/js/scene.js Three.js module: floating gold particle geometry, WebGL detection, demand-based render control, mobile skip, and pixel-ratio cap. Exports init() and resize() only. No requestAnimationFrame, no GSAP import.

Purpose: Deliver the isolated Three.js scene that Phase 5 (GSAP) will wire into its ticker loop. The module must be testable standalone — open index.html with scene.js loaded, call scene.init(), confirm particles render without touching main.js or animations.js.

Output: assets/js/scene.js — complete, production-ready module (~100 lines).
</objective>

<execution_context>
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/PROJECT.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/ROADMAP.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/STATE.md

<interfaces>
<!-- Existing token contracts from assets/css/base.css — hardcode the hex values in JS, do not read CSS at runtime -->

Z-index stack (base.css):
  --z-canvas: 0
  --z-overlay: 1
  --z-content: 2

Gold colors (hardcode in JS, do not read CSS vars at runtime):
  GOLD_BRIGHT = '#FFD700'   (--gold-bright)
  GOLD_MID    = '#C9A227'   (--gold-mid)

Mobile breakpoint:
  MOBILE_BREAKPOINT = 768   (literal, matches all @media in CSS)

Three.js source:
  CDN: https://esm.sh/three@0.169   (vendor dir is empty — use CDN import)
  Import map or top-level: import * as THREE from 'https://esm.sh/three@0.169'

WebGL detection pattern (no external lib):
  const canvas = document.createElement('canvas');
  const gl = canvas.getContext('webgl') || canvas.getContext('experimental-webgl');
  // if gl is null → WebGL unavailable

Demand-based rendering contract (consumed by Phase 5 main.js):
  // Phase 5 wires: gsap.ticker.add(() => { if (needsRender) { renderer.render(scene, camera); needsRender = false; } })
  // scene.js exposes needsRender as module-level variable; Phase 5 reads it via the render() export

Exported API that Phase 5 (main.js) will call:
  init(canvasElement)  — sets up renderer, scene, camera, particles; attaches resize listener
  resize()             — updates camera aspect + renderer size; sets needsRender = true
  render()             — called by GSAP ticker; renders one frame if needsRender is true, then resets flag
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write scene.js — Three.js particle module with WebGL detection, mobile skip, demand rendering</name>
  <files>assets/js/scene.js</files>
  <read_first>
    - assets/js/scene.js (current placeholder — full replacement)
    - assets/css/base.css (gold color values and z-index tokens for reference — hardcode values in JS)
    - CLAUDE.md (key technical rules: no rAF, pointer-events none, pixel ratio cap, mobile skip)
  </read_first>
  <behavior>
    - init(canvasEl) on mobile (window.innerWidth < 768): return immediately, set no state
    - init(canvasEl) when WebGL unavailable: return immediately, set no state, throw no error
    - init(canvasEl) on desktop with WebGL: creates BufferGeometry with 1000 vertices, assigns gold colors (#FFD700 and #C9A227 alternating), mounts renderer to canvasEl, sets needsRender = true
    - resize() on desktop: updates camera.aspect = window.innerWidth / window.innerHeight, calls camera.updateProjectionMatrix(), calls renderer.setSize(window.innerWidth, window.innerHeight), sets needsRender = true
    - render(): if needsRender is true, calls renderer.render(scene, camera) then sets needsRender = false; if needsRender is false, no-ops
    - Particle animation per-frame: in render(), before rendering, increment a time offset and apply it to particle positions (slow drift — each particle moves on a sine/cosine path using its seed offset; amplitude ~0.5 units, period ~4s)
    - module-level: no calls to requestAnimationFrame, setInterval, or setTimeout
  </behavior>
  <action>
    Replace the placeholder comment in assets/js/scene.js with a complete ES module. Structure:

    1. CONSTANTS block at top — MOBILE_BREAKPOINT = 768, PARTICLE_COUNT_DESKTOP = 1000, PARTICLE_COUNT_MOBILE = 400, GOLD_BRIGHT = '#FFD700', GOLD_MID = '#C9A227', PIXEL_RATIO_CAP = 1.5.

    2. Module-level state variables (not exported): scene, camera, renderer, particles (the Points object), clock (THREE.Clock), needsRender = false.

    3. detectWebGL() — creates a throwaway canvas, attempts getContext('webgl') then getContext('experimental-webgl'), returns boolean. No DOM side effects.

    4. init(canvasEl) — exported function:
       - Guard 1: if window.innerWidth < MOBILE_BREAKPOINT, return (per HERO-04, PERF-04).
       - Guard 2: if !detectWebGL(), return (per PERF-05).
       - Import THREE from 'https://esm.sh/three@0.169' at module top (static import, not dynamic).
       - Particle count: PARTICLE_COUNT_DESKTOP (always — init only called on desktop per Guard 1; PARTICLE_COUNT_MOBILE is reserved for future use but still declared as a constant).
       - BufferGeometry: Float32Array of size count * 3 for positions; positions are random in range [-15, 15] on x/y, [-10, 10] on z. Float32Array of size count * 3 for colors; alternate GOLD_BRIGHT and GOLD_MID per particle (use THREE.Color to get r/g/b floats).
       - Float32Array of size count for seed offsets (random 0–2π per particle) — store as geometry attribute named 'seed' for animation use.
       - Material: THREE.PointsMaterial({ size: 0.08, vertexColors: true, transparent: true, opacity: 0.75, sizeAttenuation: true }).
       - Camera: THREE.PerspectiveCamera(60, window.innerWidth / window.innerHeight, 0.1, 100); camera.position.z = 20.
       - Renderer: new THREE.WebGLRenderer({ canvas: canvasEl, alpha: true, antialias: false }); renderer.setPixelRatio(Math.min(window.devicePixelRatio, PIXEL_RATIO_CAP)); renderer.setSize(window.innerWidth, window.innerHeight); renderer.setClearColor(0x000000, 0) to keep alpha transparent.
       - Add Points to scene. Set needsRender = true.
       - Attach window 'mousemove' listener that sets needsRender = true.
       - Do NOT call requestAnimationFrame.

    5. resize() — exported function:
       - Guard: if !renderer, return (not initialised — mobile or no WebGL).
       - Update camera.aspect, call camera.updateProjectionMatrix(), renderer.setSize(window.innerWidth, window.innerHeight), set needsRender = true.

    6. render() — exported function (Phase 5 ticker will call this):
       - If !needsRender or !renderer, return.
       - Animate particles: read elapsed = clock.getDelta() (or use clock.getElapsedTime() — use getElapsedTime for absolute time). Update positions array in geometry: for each particle i, compute base position (stored separately as origPositions Float32Array initialised at init time), apply offset: pos[i*3+1] = origPositions[i*3+1] + Math.sin(clock.getElapsedTime() * 0.5 + seeds[i]) * 0.3. Mark geometry.attributes.position.needsUpdate = true.
       - Call renderer.render(scene, camera).
       - Set needsRender = false.

    Export: export { init, resize, render }.
    Do NOT export: scene, camera, renderer, needsRender, detectWebGL.
    Do NOT import GSAP, animations.js, main.js, or any local project file.
  </action>
  <verify>
    <automated>grep "export {" assets/js/scene.js && grep -c "needsRender" assets/js/scene.js && grep -c "MOBILE_BREAKPOINT" assets/js/scene.js</automated>
  </verify>
  <acceptance_criteria>
    - assets/js/scene.js exists and is not just the placeholder comment line
    - File exports exactly: init, resize, render (grep -c "export" assets/js/scene.js returns >= 1; grep "export {" shows init, resize, render)
    - File does NOT contain "requestAnimationFrame" (grep -c "requestAnimationFrame" assets/js/scene.js returns 0)
    - File does NOT contain "import.*gsap" or "import.*animations" (grep -ic "import.*gsap\|import.*animations" assets/js/scene.js returns 0)
    - File contains "MOBILE_BREAKPOINT = 768" constant
    - File contains "PARTICLE_COUNT_DESKTOP = 1000"
    - File contains "Math.min(window.devicePixelRatio" and "1.5" on the same line (PERF-03)
    - File contains "needsRender" variable
    - File contains "detectWebGL" function
    - File contains "esm.sh/three@0.169" CDN import
    - File contains "alpha: true" in WebGLRenderer options
    - File line count >= 80 (wc -l assets/js/scene.js returns >= 80)
  </acceptance_criteria>
  <done>scene.js is a complete ES module: detectWebGL guard, mobile guard, 1000 gold particles, demand-based needsRender flag, pixel ratio capped at 1.5, exports init/resize/render, zero rAF calls, zero GSAP imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN import | Three.js loaded from esm.sh/three@0.169 — external network dependency |
| window events | mousemove listener attached in init() — low risk, sets only needsRender flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | esm.sh CDN (Three.js) | accept | Static pricing site, low-value target; Three.js is widely mirrored; no user data in scope |
| T-04-02 | Denial of Service | renderer.setSize on rapid resize | accept | needsRender flag batches render calls; no unbounded loop created |
| T-04-03 | Information Disclosure | WebGL fingerprinting via detectWebGL() | accept | Detection uses a throwaway canvas, no data sent anywhere; standard industry practice |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan — Three.js loaded via CDN URL, no install step |
</threat_model>

<verification>
After execution, verify the complete plan:

1. grep -c "requestAnimationFrame" assets/js/scene.js  → must return 0
2. grep "export {" assets/js/scene.js  → must show init, resize, render
3. grep -c "MOBILE_BREAKPOINT" assets/js/scene.js  → must return >= 1
4. grep -c "needsRender" assets/js/scene.js  → must return >= 3 (declaration + set true + set false)
5. grep -c "esm.sh/three" assets/js/scene.js  → must return 1
6. grep -c "Math.min(window.devicePixelRatio" assets/js/scene.js  → must return 1
7. wc -l assets/js/scene.js (or measure line count)  → must be >= 80
</verification>

<success_criteria>
scene.js is complete and self-contained:
- init/resize/render exported; no rAF; no GSAP import
- Mobile guard (< 768px) and WebGL guard both present
- 1000 gold particles (#FFD700 / #C9A227), pixel ratio capped at 1.5
- Demand-based rendering via needsRender flag
- Three.js loaded from esm.sh/three@0.169 CDN
- Module is testable standalone: open browser console, call scene.init(canvasEl), particles appear
</success_criteria>

<output>
Create .planning/phases/04-three-js-scene/04-01-SUMMARY.md when done
</output>
