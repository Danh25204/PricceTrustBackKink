---
wave: 1
depends_on: []
files_modified:
  - index.html
  - assets/og-image.jpg
autonomous: true
requirements_addressed:
  - DATA-04
---

# Plan 06-01: OG Image + Open Graph Meta Tags

## Objective

Create a 1200×630px branded OG image at `./assets/og-image.jpg` and add all required Open Graph meta tags to `index.html` so sharing the URL in Telegram shows a proper link preview.

## Tasks

<task id="06-01-T1">
<title>Generate OG image using PowerShell + System.Drawing</title>

<action>
Generate `assets/og-image.jpg` (1200×630px) using a PowerShell script with System.Drawing. Gold-black branded image with TrustBackLink name and tagline.
</action>

<acceptance_criteria>
- `assets/og-image.jpg` exists at project root/assets/
- File is 1200×630px
- File size is ≤ 200 KB
</acceptance_criteria>
</task>

<task id="06-01-T2">
<title>Add Open Graph + Twitter Card meta tags to index.html</title>

<action>
Add OG and Twitter Card meta tags to `<head>` in `index.html`.
</action>

<acceptance_criteria>
- `og:type`, `og:title`, `og:description`, `og:image`, `og:url`, `og:locale` present
- `twitter:card` = `summary_large_image` present
- `og:image` points to `./assets/og-image.jpg`
</acceptance_criteria>
</task>

## Verification Criteria

- `assets/og-image.jpg` exists and is 1200×630
- `index.html` contains `og:image` meta tag referencing the file
- Browser DevTools shows no 404 for og-image.jpg when serving locally
