---
phase: 02-html-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03
  - HERO-01
  - HERO-02

must_haves:
  truths:
    - "Header shows TrustBackLink brand logo img + brand name text, always readable without CSS"
    - "Header contains a clickable @trustbacklink Telegram link"
    - "Hero h1 reads exactly: Dịch vụ SEO chất lượng cao — Báo giá minh bạch"
    - "Hero has a primary CTA anchor linking to tg://resolve?domain=trustbacklink"
    - "Footer shows TrustBackLink name, Telegram link, and 2026 copyright"
  artifacts:
    - path: "index.html"
      provides: "Header, hero, footer HTML"
      contains: "site-header, hero, site-footer, cta-primary"
  key_links:
    - from: "header .brand-telegram"
      to: "https://t.me/trustbacklink"
      via: "href attribute"
      pattern: "brand-telegram.*trustbacklink"
    - from: ".cta-primary"
      to: "tg://resolve?domain=trustbacklink"
      via: "href attribute"
      pattern: "cta-primary.*tg://resolve"
---

<objective>
Write the header, footer, and hero section HTML into index.html. This is Wave 1 — foundation HTML that all other plans build upon by filling the `<main>` body.

Purpose: Establishes the document outline, brand identity elements, and above-the-fold hero CTA before any content sections are added.
Output: index.html with populated `<header>`, `<section id="hero">`, `<footer>`, and the floating CTA anchor `<a id="cta-float">`. The `<main>` element is created but the service/pricing sections remain for later plans.
</objective>

<execution_context>
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/PROJECT.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/ROADMAP.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/STATE.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/phases/02-html-skeleton/02-UI-SPEC.md

<interfaces>
<!-- CSS token contract from base.css — Phase 2 HTML must anticipate these class names -->
<!-- CSS classes declared in UI-SPEC.md §CSS Class Naming Contract: -->
<!--   .site-header, .brand-name, .brand-telegram -->
<!--   .hero, .hero__tagline, .hero__sub, .cta-primary -->
<!--   .site-footer -->
<!--   #cta-float, .sr-only -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add meta description and write header + hero HTML</name>
  <files>index.html</files>

  <read_first>
    - index.html — read current state (Phase 1 scaffold); do NOT modify anything in `<head>` except adding the 2 meta tags specified below
    - .planning/phases/02-html-skeleton/02-UI-SPEC.md §HTML Document Contract, §Header, §Hero Section, §CSS Class Naming Contract, §Telegram Deeplink Specification
    - assets/css/base.css — confirm token names; Phase 2 HTML must use class names that match what Phase 3 will style against
  </read_first>

  <action>
    Replace the `<body>` content of index.html (currently only a comment). Do not modify `<head>` except to insert these two `<meta>` tags immediately before `</head>`:

    ```
    <meta name="description" content="Báo giá dịch vụ SEO chuyên nghiệp — Backlink, Blog Comment, Entity SEO. Liên hệ @trustbacklink qua Telegram.">
    <meta name="robots" content="index, follow">
    ```

    Then write the full `<body>` structure. Do NOT use any JavaScript.

    HEADER — use exactly:
    - Element: `<header class="site-header" role="banner">`
    - Logo: `<img src="./assets/images/logo.png" alt="TrustBackLink logo" class="brand-logo">`
    - Brand name: `<span class="brand-name">TrustBackLink</span>` (adjacent to img, always visible)
    - Telegram link: `<a class="brand-telegram" href="https://t.me/trustbacklink" rel="noopener" target="_blank">@trustbacklink</a>`

    HERO — use exactly:
    - Element: `<section id="hero" class="hero">`
    - h1: `<h1 class="hero__tagline">Dịch vụ SEO chất lượng cao — Báo giá minh bạch</h1>`
    - Sub-tagline: `<p class="hero__sub">Backlink • Blog Comment • Entity SEO — Đặt hàng qua Telegram, nhận báo giá trong 5 phút.</p>`
    - CTA: `<a class="cta-primary" href="tg://resolve?domain=trustbacklink" data-fallback="https://t.me/trustbacklink" rel="noopener" aria-label="Nhắn Telegram ngay">Nhắn Telegram ngay</a>`

    MAIN — create the wrapper with placeholder comments for later plans:
    - `<main>` containing `<!-- Plans 02-02, 02-03, 02-04 fill this main -->`

    FOOTER — use exactly (BRAND-03):
    - Element: `<footer class="site-footer">`
    - Line 1: `<p>TrustBackLink — Dịch vụ SEO chuyên nghiệp</p>`
    - Line 2: `<p>Telegram: <a href="https://t.me/trustbacklink" rel="noopener" target="_blank">@trustbacklink</a></p>`
    - Line 3: `<p>© 2026 TrustBackLink. All rights reserved.</p>`

    FLOATING CTA — place after `</footer>`, outside `<main>` (CTA-01):
    ```
    <a id="cta-float"
       href="tg://resolve?domain=trustbacklink"
       data-fallback="https://t.me/trustbacklink"
       rel="noopener"
       aria-label="Nhắn Telegram ngay"
       title="Nhắn Telegram ngay">
      <span aria-hidden="true">✈</span>
      <span class="sr-only">Nhắn Telegram ngay</span>
    </a>
    ```

    Constraint: No `<style>` or `<script>` tags in `<body>`.
    Constraint: All paths relative — `./assets/images/logo.png` not `/assets/images/logo.png`.
    Constraint: `lang="vi"` is already on `<html>` — do not re-add.
  </action>

  <verify>
    <automated>
      node -e "
      const fs = require('fs');
      const h = fs.readFileSync('C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/index.html','utf8');
      const checks = [
        ['site-header', h.includes('class=\"site-header\"')],
        ['brand-logo img', h.includes('class=\"brand-logo\"')],
        ['brand-name span', h.includes('class=\"brand-name\"')],
        ['brand-telegram link', h.includes('class=\"brand-telegram\"')],
        ['hero section', h.includes('id=\"hero\"')],
        ['hero__tagline h1', h.includes('class=\"hero__tagline\"')],
        ['h1 copy', h.includes('Dịch vụ SEO chất lượng cao')],
        ['hero__sub', h.includes('class=\"hero__sub\"')],
        ['cta-primary', h.includes('class=\"cta-primary\"')],
        ['tg deeplink hero', h.includes('tg://resolve?domain=trustbacklink')],
        ['data-fallback hero', h.includes('data-fallback=\"https://t.me/trustbacklink\"')],
        ['main element', h.includes('<main')],
        ['site-footer', h.includes('class=\"site-footer\"')],
        ['footer TrustBackLink text', h.includes('TrustBackLink — Dịch vụ SEO')],
        ['footer telegram link', h.includes('href=\"https://t.me/trustbacklink\"')],
        ['copyright 2026', h.includes('© 2026 TrustBackLink')],
        ['cta-float', h.includes('id=\"cta-float\"')],
        ['sr-only', h.includes('class=\"sr-only\"')],
        ['no script tag in body', !/<body[\s\S]*<script/.test(h)],
        ['relative logo path', h.includes('./assets/images/logo.png')],
        ['meta description', h.includes('Báo giá dịch vụ SEO')],
        ['meta robots', h.includes('content=\"index, follow\"')],
      ];
      let fail = 0;
      checks.forEach(([name,pass]) => { if(!pass){console.error('FAIL:',name);fail++;} });
      if(fail===0) console.log('ALL CHECKS PASSED');
      else process.exit(1);
      "
    </automated>
  </verify>

  <acceptance_criteria>
    - `class="site-header"` present on `<header role="banner">`
    - `<img src="./assets/images/logo.png" alt="TrustBackLink logo" class="brand-logo">` present
    - `<span class="brand-name">TrustBackLink</span>` present (exact casing)
    - `<a class="brand-telegram" href="https://t.me/trustbacklink"` present with `rel="noopener"`
    - `<section id="hero" class="hero">` present
    - `<h1 class="hero__tagline">Dịch vụ SEO chất lượng cao — Báo giá minh bạch</h1>` — exact Vietnamese copy, em-dash
    - `<p class="hero__sub">Backlink • Blog Comment • Entity SEO — Đặt hàng qua Telegram, nhận báo giá trong 5 phút.</p>`
    - `<a class="cta-primary" href="tg://resolve?domain=trustbacklink"` with `data-fallback="https://t.me/trustbacklink"` and `rel="noopener"`
    - `<main>` element present (empty placeholder for plans 02-02, 02-03, 02-04)
    - `<footer class="site-footer">` present with all 3 `<p>` lines
    - Footer has `href="https://t.me/trustbacklink"` link
    - `© 2026 TrustBackLink. All rights reserved.` in footer
    - `<a id="cta-float"` with `href="tg://resolve?domain=trustbacklink"` and `data-fallback` and `aria-label="Nhắn Telegram ngay"` present after `</footer>`
    - `<span class="sr-only">Nhắn Telegram ngay</span>` inside cta-float
    - No `<script>` or `<style>` tags in `<body>`
    - All asset paths use `./assets/` (relative)
    - `<meta name="description">` and `<meta name="robots">` added to `<head>`
  </acceptance_criteria>

  <done>Header (brand logo, brand name, Telegram username link), hero (h1 tagline + sub-tagline + primary CTA), footer (name + Telegram + copyright), and floating CTA anchor all exist in index.html with correct class names, exact copy, and tg:// deeplinks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram deeplink → device OS | tg:// URL is passed to the OS handler; no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Telegram href values | mitigate | All tg:// and https://t.me/ URLs are hardcoded in HTML — no dynamic construction from URL params or user input |
| T-02-02 | Information Disclosure | data-fallback attribute | accept | Attribute value is a public Telegram URL; no secret data involved |
| T-02-03 | Spoofing | @trustbacklink link text | mitigate | Link text is static hardcoded copy; no innerHTML with external data |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Phase 2 has zero package manager installs — pure HTML authoring |
</threat_model>

<verification>
Open index.html directly in a browser (file:// or local server). Without any CSS or JS loading:
1. Page title "TrustBackLink — Báo Giá Dịch Vụ SEO" visible in tab
2. Header section shows logo img (or broken img placeholder) + "TrustBackLink" text + "@trustbacklink" link
3. h1 "Dịch vụ SEO chất lượng cao — Báo giá minh bạch" is visible
4. "Nhắn Telegram ngay" link is clickable (opens Telegram on device with Telegram installed)
5. Footer visible at bottom with brand name, Telegram link, copyright
6. Page source shows no `<script>` tags in body
</verification>

<success_criteria>
- BRAND-01: Header displays logo img element + `<span class="brand-name">TrustBackLink</span>`
- BRAND-02: `<a class="brand-telegram" href="https://t.me/trustbacklink">@trustbacklink</a>` present in header
- BRAND-03: Footer has TrustBackLink name, `https://t.me/trustbacklink` link, `© 2026` copyright
- HERO-01: `<h1 class="hero__tagline">Dịch vụ SEO chất lượng cao — Báo giá minh bạch</h1>` present
- HERO-02: `<a class="cta-primary" href="tg://resolve?domain=trustbacklink"` with data-fallback present in hero section
- CTA-01: `<a id="cta-float">` with tg:// href, aria-label, sr-only span present outside `<main>`
- CTA-03: All Telegram links use tg:// primary + data-fallback https://t.me/ pattern
- Automated check script exits 0
</success_criteria>

<output>
Create `.planning/phases/02-html-skeleton/02-01-SUMMARY.md` when done.
</output>
