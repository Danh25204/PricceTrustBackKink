---
phase: 02-html-skeleton
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - index.html
autonomous: true
requirements:
  - SVC-01
  - SVC-02
  - SVC-03

must_haves:
  truths:
    - "Section id='services' exists inside <main> with h2 'Dịch vụ của chúng tôi'"
    - "Three service cards present: Backlink SEO, Blog Comment, Entity SEO"
    - "Each service card has a 2-3 line Vietnamese description in .service-card__desc"
    - "Entity card contains the exact mandatory 2-sentence SVC-03 explanation"
    - "All service cards use class='service-card' inside class='services__grid'"
  artifacts:
    - path: "index.html"
      provides: "Services section with 3 article.service-card elements"
      contains: "services, services__grid, service-card, Entity SEO"
  key_links:
    - from: "section#services"
      to: "div.services__grid"
      via: "parent-child nesting"
      pattern: "services__grid"
    - from: "article.service-card"
      to: "h3.service-card__title"
      via: "heading inside article"
      pattern: "service-card__title"
---

<objective>
Write the `<section id="services">` block inside `<main>` in index.html, containing 3 service cards (Backlink SEO, Blog Comment, Entity SEO) with full Vietnamese descriptions.

Purpose: Provides the "Dịch vụ của chúng tôi" overview section so visitors understand what each service type is before seeing pricing.
Output: Services section appended inside `<main>` in index.html; no pricing cards — those are plans 02-03 and 02-04.
</objective>

<execution_context>
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/ROADMAP.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/STATE.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/phases/02-html-skeleton/02-UI-SPEC.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/phases/02-html-skeleton/02-01-SUMMARY.md

<interfaces>
<!-- CSS class contract from UI-SPEC.md §CSS Class Naming Contract: -->
<!--   .services          — the <section> wrapper -->
<!--   .services__grid    — <div> grid container for 3 cards -->
<!--   .service-card      — <article> per service -->
<!--   .service-card__title — <h3> inside each card -->
<!--   .service-card__desc  — <p> description inside each card -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert services section with 3 service cards into main</name>
  <files>index.html</files>

  <read_first>
    - index.html — read the current state after plan 02-01 to see the `<main>` placeholder comment location; insert services section replacing or after the comment, keeping `<main>` open for plans 02-03 and 02-04
    - .planning/phases/02-html-skeleton/02-UI-SPEC.md §Services Section (SVC-01, SVC-02, SVC-03), §CSS Class Naming Contract
  </read_first>

  <action>
    Inside `<main>` in index.html, insert the services section. Position: before the plans 02-03/02-04 placeholder comment (or replace the comment; keep `<main>` open).

    Use exactly this structure:

    SERVICES SECTION:
    ```
    <section id="services" class="services">
      <h2>Dịch vụ của chúng tôi</h2>
      <div class="services__grid">

        <!-- Backlink SEO -->
        <article class="service-card">
          <h3 class="service-card__title">Backlink SEO</h3>
          <p class="service-card__desc">Xây dựng hệ thống backlink chất lượng cao từ các website có DA thực sự,
          giúp tăng thứ hạng từ khóa bền vững và tăng domain authority.
          Tỷ lệ do-follow đảm bảo, đúng niche, không spam.</p>
        </article>

        <!-- Blog Comment -->
        <article class="service-card">
          <h3 class="service-card__title">Blog Comment</h3>
          <p class="service-card__desc">Đặt bình luận tự nhiên trên các blog có lượng traffic thực,
          tạo tín hiệu liên kết đa dạng cho profile backlink của bạn.
          Tỷ lệ phê duyệt cao, phù hợp niche, thời gian xử lý nhanh.</p>
        </article>

        <!-- Entity SEO — mandatory 2-sentence explanation per SVC-03 -->
        <article class="service-card">
          <h3 class="service-card__title">Entity SEO</h3>
          <p class="service-card__desc">Entity SEO giúp Google nhận diện thương hiệu bạn là một thực thể có thật,
          tăng uy tín domain bền vững.
          Chúng tôi xây dựng hồ sơ thực thể trên các nền tảng uy tín,
          kết nối thông tin thương hiệu nhất quán trên toàn bộ web.</p>
        </article>

      </div>
    </section>
    ```

    Constraints:
    - Do NOT close `<main>` — plans 02-03 and 02-04 add more sections inside it.
    - No `<style>` or `<script>` tags.
    - The mandatory SVC-03 first 2 sentences must appear verbatim:
      "Entity SEO giúp Google nhận diện thương hiệu bạn là một thực thể có thật, tăng uy tín domain bền vững."
    - Keep existing header, hero, footer, #cta-float untouched.
  </action>

  <verify>
    <automated>
      node -e "
      const fs = require('fs');
      const h = fs.readFileSync('C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/index.html','utf8');
      const checks = [
        ['section#services', h.includes('id=\"services\"')],
        ['services class', h.includes('class=\"services\"')],
        ['h2 services heading', h.includes('<h2>Dịch vụ của chúng tôi</h2>')],
        ['services__grid', h.includes('class=\"services__grid\"')],
        ['3 service-card articles', (h.match(/class=\"service-card\"/g)||[]).length >= 3],
        ['Backlink SEO h3', h.includes('>Backlink SEO<')],
        ['Blog Comment h3', h.includes('>Blog Comment<')],
        ['Entity SEO h3', h.includes('>Entity SEO<')],
        ['service-card__title class', h.includes('class=\"service-card__title\"')],
        ['service-card__desc class', h.includes('class=\"service-card__desc\"')],
        ['SVC-03 sentence 1', h.includes('Entity SEO giúp Google nhận diện thương hiệu bạn là một thực thể có thật')],
        ['SVC-03 sentence 2', h.includes('tăng uy tín domain bền vững')],
        ['backlink desc', h.includes('domain authority')],
        ['blog comment desc', h.includes('Tỷ lệ phê duyệt cao')],
        ['main still open', /<main[\s\S]*<section id=\"services\"/.test(h)],
        ['header preserved', h.includes('class=\"site-header\"')],
        ['footer preserved', h.includes('class=\"site-footer\"')],
      ];
      let fail = 0;
      checks.forEach(([name,pass]) => { if(!pass){console.error('FAIL:',name);fail++;} });
      if(fail===0) console.log('ALL CHECKS PASSED');
      else process.exit(1);
      "
    </automated>
  </verify>

  <acceptance_criteria>
    - `<section id="services" class="services">` inside `<main>`
    - `<h2>Dịch vụ của chúng tôi</h2>` as section heading
    - `<div class="services__grid">` wrapping all 3 cards
    - 3 `<article class="service-card">` elements
    - Each article has `<h3 class="service-card__title">` and `<p class="service-card__desc">`
    - Backlink card title: `>Backlink SEO<` and description includes "domain authority" and "do-follow"
    - Blog Comment card title: `>Blog Comment<` and description includes "Tỷ lệ phê duyệt cao"
    - Entity card title: `>Entity SEO<` and description contains EXACTLY: "Entity SEO giúp Google nhận diện thương hiệu bạn là một thực thể có thật, tăng uy tín domain bền vững." (verbatim, SVC-03)
    - `<main>` is NOT closed — remaining placeholder for plans 02-03 and 02-04
    - Header, hero, footer, #cta-float from plan 02-01 remain intact
    - No `<style>` or `<script>` tags added
  </acceptance_criteria>

  <done>Services section with 3 articles (Backlink SEO, Blog Comment, Entity SEO) is inside `<main>` with correct class names and exact Vietnamese copy including the mandatory SVC-03 Entity explanation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static HTML render | No external data; all copy is hardcoded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | Service card copy | accept | All text is hardcoded; no innerHTML from external input; XSS not applicable |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Phase 2 plan 02 has zero package installs — pure HTML authoring |
</threat_model>

<verification>
Open index.html in a browser. Without CSS or JS:
1. "Dịch vụ của chúng tôi" heading visible below hero section
2. Three cards visible side-by-side (or stacked on mobile browser default): Backlink SEO, Blog Comment, Entity SEO
3. Each card shows 2-3 lines of Vietnamese description text
4. Entity card description contains "thực thể có thật" and "tăng uy tín domain bền vững"
5. Page structure: header → hero → services → (main still open for next plans) → footer
</verification>

<success_criteria>
- SVC-01: `<section id="services" class="services">` with h2 "Dịch vụ của chúng tôi" and 3 service-card articles
- SVC-02: Each service card has `.service-card__desc` `<p>` with 2-3 line Vietnamese description
- SVC-03: Entity card description contains exact text "Entity SEO giúp Google nhận diện thương hiệu bạn là một thực thể có thật, tăng uy tín domain bền vững."
- Automated check script exits 0
</success_criteria>

<output>
Create `.planning/phases/02-html-skeleton/02-02-SUMMARY.md` when done.
</output>
