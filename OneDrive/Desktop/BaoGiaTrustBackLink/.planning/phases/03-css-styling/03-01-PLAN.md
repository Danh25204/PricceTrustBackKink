---
phase: 03-css-styling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - assets/css/layout.css
autonomous: true
requirements:
  - ANIM-07

must_haves:
  truths:
    - "index.html loads layout.css, components.css, and animations.css via <link> tags"
    - "Header shows brand name and Telegram link in a horizontal flex row against a near-black background"
    - "Hero section fills at least 60vh, centers content vertically, with the dark overlay ensuring text legibility"
    - "Services grid renders 3 columns on desktop, 1 column on mobile (< 768px)"
    - "Each pricing section has a visible heading with a gold accent underline or color treatment"
    - "Footer sits at bottom with muted text, separated from content by a border"
  artifacts:
    - path: "index.html"
      provides: "Three new <link> stylesheet tags for layout.css, components.css, animations.css"
      contains: "link rel=\"stylesheet\" href=\"./assets/css/layout.css\""
    - path: "assets/css/layout.css"
      provides: "All structural layout rules: header, hero, sections, services grid, footer"
      min_lines: 80
  key_links:
    - from: "assets/css/layout.css"
      to: "index.html"
      via: "<link rel=\"stylesheet\"> in <head>"
      pattern: "link.*layout\\.css"
    - from: ".site-header"
      to: "layout.css"
      via: "CSS rule targeting .site-header class"
      pattern: "\\.site-header"
    - from: ".services__grid"
      to: "layout.css"
      via: "CSS grid declaration"
      pattern: "\\.services__grid"
---

<objective>
Wire CSS files into index.html and implement all structural layout rules in layout.css.

Purpose: Phase 3 cannot render until index.html references the three new CSS files. Layout.css establishes the page skeleton — header bar, hero section with overlay, sections, services grid, and footer — that all cards and components sit inside.

Output: index.html with three new `<link>` tags; layout.css fully populated with structural rules.
</objective>

<execution_context>
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/PROJECT.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/ROADMAP.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/STATE.md

<interfaces>
<!-- Frozen CSS token contract — base.css Block 1–6. Use var() references exactly as declared. -->
<!-- Colors: --gold-bright (#FFD700), --gold-mid (#C9A227), --gold-dim (#8B6914) -->
<!-- Colors: --black-rich (#0A0A0A), --black-card (#111111), --black-border (#1E1E1E) -->
<!-- Colors: --text-primary (#F5F0E8), --text-muted (#8A8070) -->
<!-- Overlay: --overlay-dark (rgba(0,0,0,0.65)) -->
<!-- Glow: --gold-glow (rgba(255,215,0,0.35)), --gold-glow-dim (rgba(255,215,0,0.15)) -->
<!-- Spacing: --space-xs(4px) --space-sm(8px) --space-md(16px) --space-lg(24px) -->
<!--          --space-xl(32px) --space-2xl(48px) --space-3xl(64px) --cta-touch(56px) -->
<!-- Typography: --text-sm(14px) --text-base(16px) --text-lg(28px) --text-xl(40px) -->
<!-- Z-index: --z-canvas(0) --z-overlay(1) --z-content(2) --z-float(100) -->
<!-- Card: --card-featured-border, --card-featured-shadow, --card-default-border -->
<!--       --card-bg (linear-gradient 135deg), --card-hover-lift (translateY(-4px)) -->
<!-- BREAKPOINTS: Always use literal 768px in @media — var(--bp-mobile) is docs-only -->

<!-- HTML classes from index.html (Phase 2 output — do not modify HTML structure): -->
<!-- Header: .site-header (flex row), .brand-logo (img), .brand-name (span), .brand-telegram (a) -->
<!-- Hero: section#hero.hero, h1.hero__tagline, p.hero__sub, a.cta-primary -->
<!-- Services: section#services.services, div.services__grid, article.service-card -->
<!--   h3.service-card__title, p.service-card__desc -->
<!-- Pricing sections: section.pricing (id=backlink/blog-comment/entity) -->
<!--   div.pricing__grid, article.pricing-card, article.pricing-card.pricing-card--featured -->
<!-- Contact: section#contact -->
<!-- Footer: footer.site-footer -->
<!-- Floating: a#cta-float (outside main, after footer) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS link tags to index.html</name>
  <files>index.html</files>
  <read_first>
    - C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/index.html (read current <head> to find the exact line after base.css link)
    - C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/base.css (confirm token names before referencing)
  </read_first>
  <action>
    In index.html, locate the existing `<link rel="stylesheet" href="./assets/css/base.css">` line inside `<head>`. Immediately after that line, insert three new `<link>` tags in this exact order:

    1. `<link rel="stylesheet" href="./assets/css/layout.css">`
    2. `<link rel="stylesheet" href="./assets/css/components.css">`
    3. `<link rel="stylesheet" href="./assets/css/animations.css">`

    All paths must be relative (`./assets/css/`) — no root-relative `/assets/` paths. All filenames lowercase.

    Do NOT modify any other part of index.html — no HTML structure changes, no class changes, no content changes.
  </action>
  <verify>
    <automated>grep -c "assets/css/layout.css" "C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/index.html" && grep -c "assets/css/components.css" "C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/index.html" && grep -c "assets/css/animations.css" "C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/index.html"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `link rel="stylesheet" href="./assets/css/layout.css"`
    - index.html contains `link rel="stylesheet" href="./assets/css/components.css"`
    - index.html contains `link rel="stylesheet" href="./assets/css/animations.css"`
    - All three new link tags appear after the base.css link and before `</head>`
    - No other lines in index.html were changed
  </acceptance_criteria>
  <done>index.html `<head>` has four CSS link tags in order: base.css, layout.css, components.css, animations.css — all with relative ./assets/css/ paths</done>
</task>

<task type="auto">
  <name>Task 2: Write layout.css — header, hero, sections, services grid, footer</name>
  <files>assets/css/layout.css</files>
  <read_first>
    - C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/layout.css (confirm currently empty before writing)
    - C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/base.css (token reference — use var() exactly as declared)
    - C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/index.html (confirm exact class names on header, hero, section, footer elements)
  </read_first>
  <action>
    Write layout.css with the following sections in order. Use var() token references throughout — never hardcode color hex values or pixel values that have a token.

    SECTION 1 — SITE HEADER (.site-header):
    - `display: flex; align-items: center; justify-content: space-between`
    - `padding: var(--space-md) var(--space-xl)`
    - `background: var(--black-rich)`
    - `border-bottom: 1px solid var(--black-border)`
    - `position: sticky; top: 0; z-index: var(--z-content)`
    - `.brand-logo`: `height: 32px; width: auto` (gracefully handles broken img — no display:none)
    - `.brand-name`: `font-family: 'Playfair Display', serif; font-size: var(--text-lg); color: var(--text-primary)`
    - `.brand-telegram`: `color: var(--gold-bright); font-size: var(--text-sm); text-decoration: none`
    - `.brand-telegram:hover`: `text-decoration: underline`

    SECTION 2 — HERO SECTION (#hero, .hero):
    - `min-height: 60vh; display: flex; flex-direction: column; align-items: center; justify-content: center`
    - `text-align: center`
    - `padding: var(--space-3xl) var(--space-xl)`
    - `background: var(--black-rich)`
    - `position: relative` (for z-index context — overlay comes in Phase 4 via canvas; for now the section background is sufficient)
    - `.hero__tagline`: `font-family: 'Playfair Display', serif; font-size: var(--text-xl); color: var(--text-primary); line-height: 1.1; margin-bottom: var(--space-lg)`
    - `.hero__sub`: `font-size: var(--text-base); color: var(--text-muted); max-width: 560px; margin-bottom: var(--space-xl)`
    - `.cta-primary`:
      - `display: inline-block`
      - `padding: var(--space-md) var(--space-xl)`
      - `background: linear-gradient(135deg, var(--gold-bright) 0%, var(--gold-mid) 100%)`
      - `color: var(--black-rich)`
      - `font-weight: 600; font-size: var(--text-base)`
      - `border-radius: 4px; text-decoration: none`
      - `min-height: var(--cta-touch); line-height: var(--cta-touch)`
    - `.cta-primary:hover`: `opacity: 0.9`

    SECTION 3 — OVERLAY LAYER (CRITICAL — dark overlay between canvas z-index and content):
    Add the overlay as a `::before` pseudo-element on `.hero` (and reuse for `.pricing` sections):
    - `.hero::before`:
      - `content: ''; position: absolute; inset: 0`
      - `background: var(--overlay-dark)`
      - `z-index: var(--z-overlay)` (sits above --z-canvas:0, below --z-content:2)
      - `pointer-events: none`
    - `.hero > *`: `position: relative; z-index: var(--z-content)` (ensures all hero children are above the overlay)

    SECTION 4 — MAIN SECTIONS (general .pricing, .services, #contact):
    - `.pricing, .services, #contact`:
      - `padding: var(--space-3xl) var(--space-xl)`
      - `position: relative`
    - `.pricing h2, .services h2, #contact h2`:
      - `font-family: 'Playfair Display', serif; font-size: var(--text-lg); color: var(--gold-bright)`
      - `margin-bottom: var(--space-2xl); text-align: center`
    - Alternate section background: `.pricing:nth-child(odd)` gets `background: var(--black-rich)`, `.pricing:nth-child(even)` gets `background: #0D0D0D` (one shade lighter — this value is hardcode-allowed as it is not defined as a token)

    SECTION 5 — SERVICES GRID (.services__grid):
    - `display: grid`
    - `grid-template-columns: repeat(3, 1fr)`
    - `gap: var(--space-xl)`
    - `.service-card`:
      - `background: var(--card-bg)`
      - `border: var(--card-default-border)`
      - `border-radius: 8px`
      - `padding: var(--space-xl)`
    - `.service-card__title`:
      - `font-family: 'Playfair Display', serif; font-size: var(--text-lg); color: var(--gold-bright)`
      - `margin-bottom: var(--space-md)`
    - `.service-card__desc`: `color: var(--text-muted); font-size: var(--text-base); line-height: 1.6`
    - `@media (max-width: 768px)`: `.services__grid { grid-template-columns: 1fr }`

    SECTION 6 — PRICING GRID (.pricing__grid):
    - `display: grid`
    - `grid-template-columns: repeat(auto-fit, minmax(220px, 1fr))`
    - `gap: var(--space-xl)`
    - `@media (max-width: 768px)`: `.pricing__grid { grid-template-columns: 1fr }`

    SECTION 7 — CONTACT SECTION (#contact):
    - `text-align: center`
    - `#contact p`: `color: var(--text-muted); margin-bottom: var(--space-lg); max-width: 480px; margin-left: auto; margin-right: auto`

    SECTION 8 — FOOTER (.site-footer):
    - `padding: var(--space-2xl) var(--space-xl)`
    - `background: var(--black-rich)`
    - `border-top: 1px solid var(--black-border)`
    - `text-align: center`
    - `.site-footer p`: `color: var(--text-muted); font-size: var(--text-sm); line-height: 2`
    - `.site-footer a`: `color: var(--gold-bright); text-decoration: none`
    - `.site-footer a:hover`: `text-decoration: underline`

    SECTION 9 — SCREEN-READER UTILITY (.sr-only):
    - `position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip: rect(0,0,0,0); white-space: nowrap; border: 0`
    (Required for floating CTA .sr-only span and sr-only price spans in pricing cards)
  </action>
  <verify>
    <automated>grep -c "\.site-header" "C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/layout.css" && grep -c "\.services__grid" "C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/layout.css" && grep -c "\.pricing__grid" "C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/layout.css" && grep -c "\.site-footer" "C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/layout.css" && grep -c "var(--overlay-dark)" "C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/layout.css"</automated>
  </verify>
  <acceptance_criteria>
    - layout.css contains `.site-header {` rule with `display: flex`
    - layout.css contains `.hero` rule with `min-height: 60vh`
    - layout.css contains `.hero::before` rule with `background: var(--overlay-dark)` and `z-index: var(--z-overlay)`
    - layout.css contains `.hero > *` rule with `z-index: var(--z-content)`
    - layout.css contains `.services__grid` rule with `display: grid` and `grid-template-columns: repeat(3, 1fr)`
    - layout.css contains `.pricing__grid` rule with `display: grid` and `grid-template-columns: repeat(auto-fit, minmax(220px, 1fr))`
    - layout.css contains `.site-footer` rule with `border-top: 1px solid var(--black-border)`
    - layout.css contains `.sr-only` rule with `position: absolute; width: 1px`
    - layout.css contains `@media (max-width: 768px)` (literal — not var())
    - layout.css contains no hardcoded color hex values except `#0D0D0D` (the one non-tokenized section shade)
    - layout.css uses `var(--gold-bright)`, `var(--text-primary)`, `var(--black-rich)`, `var(--overlay-dark)` — all via var()
  </acceptance_criteria>
  <done>layout.css is fully populated; opening index.html in a browser shows the header, hero, services section, pricing sections, and footer in a structured black-background layout with gold headings and the dark overlay in the hero section</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static file → browser | All CSS is served as static files; no user input, no server-side code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | layout.css / index.html | accept | Files are static; no write vector from browser; served over HTTPS on GitHub Pages |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Phase 3 has zero package-manager installs — pure CSS/HTML authoring only |
</threat_model>

<verification>
1. Open index.html in a browser — page must load without 404 errors in the Network tab for layout.css, components.css, animations.css
2. Verify the `.site-header` is sticky at top with `position: sticky`
3. Verify the hero `::before` pseudo-element has `background: rgba(0,0,0,0.65)` in DevTools (dark overlay present)
4. Verify `.services__grid` renders 3 columns at 1280px viewport width
5. Verify `@media (max-width: 768px)` collapses `.services__grid` to 1 column
6. Verify `.sr-only` class visually hides price spans without removing from DOM
</verification>

<success_criteria>
- index.html has four CSS links in `<head>`: base.css, layout.css, components.css, animations.css
- layout.css contains all nine sections (site-header, hero, hero overlay, general sections, services grid, pricing grid, contact, footer, sr-only utility)
- Dark overlay (`var(--overlay-dark)`) applied via `.hero::before` at `z-index: var(--z-overlay)` — sits above canvas placeholder z-index (0), below content z-index (2)
- No `var(--bp-mobile)` appears in any @media query — only literal `768px`
- No hardcoded color hex values except `#0D0D0D` for the alternate pricing section shade
</success_criteria>

<output>
Create `.planning/phases/03-css-styling/03-01-SUMMARY.md` when done
</output>
