---
phase: 03-css-styling
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - assets/css/components.css
  - assets/css/animations.css
autonomous: true
requirements:
  - ANIM-07

must_haves:
  truths:
    - "Pricing cards have a dark gradient background and a visible border"
    - "Featured cards (pricing-card--featured) have a gold border and gold glow box-shadow — visually distinct from non-featured"
    - "The .badge-featured badge is absolutely positioned in the top-right corner of the featured card"
    - "All pricing cards lift (translateY(-4px)) and increase glow on CSS :hover — no JavaScript involved"
    - "The floating CTA (#cta-float) is fixed at bottom-right corner, 56x56px minimum, above all content"
    - "Pricing grid is 1 column on mobile (< 768px), multi-column on desktop"
    - "animations.css provides all :hover transition timing — components.css declares base states"
  artifacts:
    - path: "assets/css/components.css"
      provides: "Pricing card base styles, featured treatment, badge, btn-cta, contact CTA, floating CTA"
      min_lines: 80
    - path: "assets/css/animations.css"
      provides: "CSS transition for hover lift and glow on .pricing-card; reduced-motion is already handled by base.css"
      min_lines: 15
  key_links:
    - from: ".pricing-card--featured"
      to: "components.css"
      via: "CSS rule for border and box-shadow"
      pattern: "\\.pricing-card--featured"
    - from: ".pricing-card:hover"
      to: "animations.css"
      via: "CSS transition rule"
      pattern: "\\.pricing-card:hover"
    - from: "#cta-float"
      to: "components.css"
      via: "position: fixed; bottom: var(--space-xl); right: var(--space-xl)"
      pattern: "#cta-float"
---

<objective>
Write components.css (pricing cards, featured treatment, badge, CTA buttons, floating CTA) and animations.css (hover lift transitions) to complete the luxury gold-black visual treatment.

Purpose: This plan delivers the visual core of the page — the pricing card grid is the primary conversion surface. Featured cards must stand out immediately. The hover lift is a pure-CSS interaction that makes the page feel premium without any JavaScript.

Output: components.css and animations.css fully populated; page is shippable as MVP on completion.
</objective>

<execution_context>
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/PROJECT.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/ROADMAP.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/STATE.md
@C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/.planning/phases/03-css-styling/03-01-SUMMARY.md

<interfaces>
<!-- Frozen CSS token contract — base.css Block 1–6 -->
<!-- Card tokens (Block 5):
     --card-featured-border: 1px solid var(--gold-bright)
     --card-featured-shadow: 0 0 24px var(--gold-glow)
     --card-default-border:  1px solid var(--black-border)
     --card-bg:              linear-gradient(135deg, var(--black-card) 0%, #0D0D0D 100%)
     --card-hover-lift:      translateY(-4px)                                          -->
<!-- Colors: --gold-bright(#FFD700), --gold-mid(#C9A227), --gold-dim(#8B6914)          -->
<!-- Colors: --black-rich(#0A0A0A), --black-card(#111111), --black-border(#1E1E1E)     -->
<!-- Colors: --text-primary(#F5F0E8), --text-muted(#8A8070)                            -->
<!-- Glow:   --gold-glow(rgba(255,215,0,0.35)), --gold-glow-dim(rgba(255,215,0,0.15)) -->
<!-- Spacing: --space-xs(4px) --space-sm(8px) --space-md(16px) --space-lg(24px)        -->
<!--          --space-xl(32px) --space-2xl(48px) --cta-touch(56px)                     -->
<!-- Z-index: --z-float(100)                                                            -->
<!-- BREAKPOINTS: Always literal 768px in @media — never var(--bp-mobile)              -->

<!-- HTML classes from index.html (Phase 2 — do not modify):
     Pricing card:       article.pricing-card
     Featured modifier:  article.pricing-card.pricing-card--featured
     Badge:              span.badge-featured   (inside featured card, first child)
     Tier heading:       h3.pricing-card__tier
     Price container:    p.price  (contains strong + .sr-only)
     Per-unit label:     p.price-per-unit
     Feature list:       ul.features > li
     CTA button:         a.btn-cta
     Large CTA variant:  a.btn-cta.btn-cta--large   (contact section)
     Floating CTA:       a#cta-float (outside main, position:fixed)
     Contact section:    section#contact.contact-cta (or #contact per index.html)     -->

<!-- layout.css (plan 03-01) provides:
     .pricing__grid   — display:grid, gap:var(--space-xl), auto-fit minmax(220px,1fr)
     @media (max-width:768px) .pricing__grid — grid-template-columns:1fr              -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write components.css — pricing cards, featured treatment, badge, CTA buttons, floating CTA</name>
  <files>assets/css/components.css</files>
  <read_first>
    - C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/components.css (confirm currently empty before writing)
    - C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/base.css (confirm card token names exactly as declared)
    - C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/index.html (read lines 71–160 to confirm exact class names on pricing cards and badge placement)
  </read_first>
  <action>
    Write components.css with the following sections in order. All color/spacing values via var() — no hardcoded hex values except where explicitly noted below.

    SECTION 1 — BASE PRICING CARD (.pricing-card):
    - `position: relative` (required so .badge-featured absolute positioning is contained)
    - `background: var(--card-bg)`
    - `border: var(--card-default-border)`
    - `border-radius: 8px`
    - `padding: var(--space-xl)`
    - `display: flex; flex-direction: column`
    - `gap: var(--space-md)`

    SECTION 2 — FEATURED CARD MODIFIER (.pricing-card--featured):
    - `border: var(--card-featured-border)` — this resolves to `1px solid var(--gold-bright)`
    - `box-shadow: var(--card-featured-shadow)` — this resolves to `0 0 24px var(--gold-glow)`
    - `z-index: 1` (featured card sits slightly above sibling cards in the grid)

    SECTION 3 — FEATURED BADGE (.badge-featured):
    - `position: absolute; top: var(--space-md); right: var(--space-md)`
    - `background: var(--gold-bright)`
    - `color: var(--black-rich)`
    - `font-size: var(--text-sm); font-weight: 600`
    - `padding: var(--space-xs) var(--space-sm)`
    - `border-radius: 4px`
    - `text-transform: uppercase; letter-spacing: 0.05em`

    SECTION 4 — CARD CONTENT ELEMENTS:
    - `.pricing-card__tier`:
      - `font-family: 'Playfair Display', serif; font-size: var(--text-lg)`
      - `color: var(--text-primary)`
      - `padding-right: var(--space-2xl)` (avoid overlap with .badge-featured on featured cards)
    - `.price`:
      - `font-size: var(--text-xl); font-weight: 700`
      - `color: var(--gold-bright)`
      - `line-height: 1`
    - `.price strong`:
      - `font-size: inherit; color: inherit`
    - `.price-per-unit`:
      - `font-size: var(--text-sm); color: var(--text-muted)`
    - `.features`:
      - `list-style: none; padding: 0`
      - `display: flex; flex-direction: column; gap: var(--space-sm)`
      - `flex: 1` (pushes .btn-cta to bottom of flex column card)
    - `.features li`:
      - `font-size: var(--text-sm); color: var(--text-muted)`
      - `padding-left: var(--space-md)`
      - `position: relative`
    - `.features li::before`:
      - `content: '✓'; position: absolute; left: 0`
      - `color: var(--gold-dim)`

    SECTION 5 — CTA BUTTONS (.btn-cta):
    - `display: block; text-align: center`
    - `padding: var(--space-md) var(--space-lg)`
    - `background: linear-gradient(135deg, var(--gold-bright) 0%, var(--gold-mid) 100%)`
    - `color: var(--black-rich)`
    - `font-weight: 600; font-size: var(--text-base)`
    - `border-radius: 4px; text-decoration: none`
    - `min-height: var(--cta-touch); line-height: var(--cta-touch)`
    - `margin-top: auto` (ensures button always sits at card bottom)
    - `.btn-cta:hover`: `opacity: 0.9` (simple hover state; animations.css adds transition)
    - `.btn-cta--large`:
      - `padding: var(--space-lg) var(--space-2xl)`
      - `font-size: var(--text-lg)`
      - `border-radius: 6px`
      - `display: inline-block`

    SECTION 6 — FLOATING CTA (#cta-float):
    - `position: fixed`
    - `bottom: var(--space-xl); right: var(--space-xl)`
    - `z-index: var(--z-float)`
    - `width: var(--cta-touch); height: var(--cta-touch)`
    - `background: linear-gradient(135deg, var(--gold-bright) 0%, var(--gold-mid) 100%)`
    - `color: var(--black-rich)`
    - `border-radius: 50%`
    - `display: flex; align-items: center; justify-content: center`
    - `font-size: var(--text-lg)`
    - `text-decoration: none`
    - `box-shadow: 0 4px 16px var(--gold-glow)`
    - `#cta-float:hover`: `transform: scale(1.08)` (brief scale on hover; transition in animations.css)

    SECTION 7 — CONTACT SECTION CONTENT (#contact .btn-cta):
    No additional selectors needed — `.btn-cta` base styles already cover this. Only add:
    - `#contact`: `text-align: center` (already in layout.css — do not duplicate; skip if layout.css covers it)
    - `.contact-note` (if used in HTML): `color: var(--text-muted); font-size: var(--text-sm); margin-top: var(--space-md)`

    Note: Check index.html for `.contact-note` vs `.contact__note` — use whichever class is actually present in the DOM.
  </action>
  <verify>
    <automated>grep -c "\.pricing-card--featured" "C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/components.css" && grep -c "\.badge-featured" "C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/components.css" && grep -c "#cta-float" "C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/components.css" && grep -c "var(--card-featured-border)" "C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/components.css"</automated>
  </verify>
  <acceptance_criteria>
    - components.css contains `.pricing-card {` rule with `background: var(--card-bg)`, `border: var(--card-default-border)`, `position: relative`
    - components.css contains `.pricing-card--featured {` rule with `border: var(--card-featured-border)` and `box-shadow: var(--card-featured-shadow)`
    - components.css contains `.badge-featured {` rule with `position: absolute`, `top: var(--space-md)`, `right: var(--space-md)`, `background: var(--gold-bright)`
    - components.css contains `.price {` with `color: var(--gold-bright)`
    - components.css contains `.btn-cta {` with `background: linear-gradient(135deg, var(--gold-bright) 0%, var(--gold-mid) 100%)` and `min-height: var(--cta-touch)`
    - components.css contains `#cta-float {` with `position: fixed`, `z-index: var(--z-float)`, `width: var(--cta-touch)`, `height: var(--cta-touch)`, `border-radius: 50%`
    - components.css contains `.features li::before {` with `content: '✓'` and `color: var(--gold-dim)`
    - No hardcoded color hex values in components.css (all colors via var())
  </acceptance_criteria>
  <done>components.css is fully populated; the pricing cards render with dark gradient backgrounds, the featured cards have a visible gold border and glow, the badge is positioned top-right, and the floating CTA is fixed at bottom-right</done>
</task>

<task type="auto">
  <name>Task 2: Write animations.css — hover lift transition and floating CTA transition (ANIM-07)</name>
  <files>assets/css/animations.css</files>
  <read_first>
    - C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/animations.css (confirm currently empty before writing)
    - C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/base.css (confirm --card-hover-lift token value is `translateY(-4px)` and confirm reduced-motion media query is already in base.css Block 6)
    - C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/components.css (confirm .pricing-card and #cta-float hover states are declared so transitions apply correctly)
  </read_first>
  <action>
    Write animations.css. This file ONLY declares transition timing and transform states — it does NOT redeclare base styles already in components.css.

    Per ANIM-07: pricing cards must lift on hover via CSS only (no JavaScript). The `transform: var(--card-hover-lift)` token resolves to `translateY(-4px)`.

    SECTION 1 — PRICING CARD HOVER TRANSITION:
    Add `transition` property to `.pricing-card` base state (so the transition applies to both the transform and box-shadow):
    - `.pricing-card { transition: transform 0.25s ease, box-shadow 0.25s ease }`

    Note: This rule extends `.pricing-card` declared in components.css. CSS cascade is fine — both files target the same selector; properties from animations.css are additive (only `transition` is declared here).

    SECTION 2 — PRICING CARD HOVER STATE:
    - `.pricing-card:hover`:
      - `transform: var(--card-hover-lift)` — resolves to `translateY(-4px)`
      - `box-shadow: 0 8px 32px var(--gold-glow-dim)` (non-featured card gets a subtle glow lift)
    - `.pricing-card--featured:hover`:
      - `transform: var(--card-hover-lift)`
      - `box-shadow: 0 8px 40px var(--gold-glow)` (featured card gets the full gold glow on hover, stronger than default)

    SECTION 3 — FLOATING CTA TRANSITION:
    - `#cta-float { transition: transform 0.2s ease, box-shadow 0.2s ease }`
    (The `#cta-float:hover { transform: scale(1.08) }` state is declared in components.css; this transition makes it smooth)

    SECTION 4 — BTN-CTA TRANSITION:
    - `.btn-cta { transition: opacity 0.2s ease }`
    (The `.btn-cta:hover { opacity: 0.9 }` hover state is in components.css; this adds the timing)

    NOTE on reduced-motion: base.css Block 6 already has:
    `@media (prefers-reduced-motion: reduce) { *, *::before, *::after { animation-duration: 0.01ms !important; transition-duration: 0.01ms !important; } }`
    This blanket rule already disables all transitions declared in this file. Do NOT duplicate the reduced-motion media query in animations.css.
  </action>
  <verify>
    <automated>grep -c "\.pricing-card:hover" "C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/animations.css" && grep -c "var(--card-hover-lift)" "C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/animations.css" && grep -c "transition" "C:/Users/thanh/OneDrive/Desktop/BaoGiaTrustBackLink/assets/css/animations.css"</automated>
  </verify>
  <acceptance_criteria>
    - animations.css contains `.pricing-card { transition: transform 0.25s ease, box-shadow 0.25s ease }` (transition declaration on base state)
    - animations.css contains `.pricing-card:hover {` rule with `transform: var(--card-hover-lift)` (not hardcoded `translateY(-4px)`)
    - animations.css contains `.pricing-card--featured:hover {` rule with stronger `box-shadow: 0 8px 40px var(--gold-glow)`
    - animations.css contains `#cta-float { transition: transform 0.2s ease` rule
    - animations.css contains `.btn-cta { transition: opacity 0.2s ease }` rule
    - animations.css does NOT redeclare the `prefers-reduced-motion` media query (already in base.css)
    - animations.css does NOT hardcode `translateY(-4px)` — uses `var(--card-hover-lift)` exclusively
    - No CSS hover effects in this file require JavaScript — pure CSS :hover selectors only
    - Manually hovering a non-featured card in browser shows it lift 4px upward with subtle gold shadow
    - Manually hovering a featured card in browser shows it lift 4px with stronger gold glow
  </acceptance_criteria>
  <done>animations.css delivers ANIM-07: all pricing card hover lift effects work via pure CSS :hover transitions; no JavaScript is required for any hover state on the page</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static file → browser | CSS files are static; no user input processed, no server-side execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | components.css / animations.css | accept | Files are static assets; no write vector from client; served over HTTPS on GitHub Pages |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Phase 3 plan 02 has zero package-manager installs — pure CSS authoring only |
</threat_model>

<verification>
1. Open index.html in browser — 0 console errors, 0 404s in Network tab
2. Inspect a non-featured pricing card: border should be `1px solid #1E1E1E` (var(--black-border)), no box-shadow
3. Inspect a featured pricing card (.pricing-card--featured): border should be `1px solid #FFD700` (var(--gold-bright)), box-shadow `0 0 24px rgba(255,215,0,0.35)`
4. Hover a pricing card — card lifts 4px (translateY(-4px)) with smooth 0.25s transition, confirmed in DevTools Computed tab
5. Hover a featured card — stronger gold glow (0 8px 40px rgba(255,215,0,0.35)) appears on lift
6. Inspect .badge-featured: `position: absolute; top: 16px; right: 16px; background: #FFD700`
7. Inspect #cta-float: `position: fixed; bottom: 32px; right: 32px; width: 56px; height: 56px; border-radius: 50%; z-index: 100`
8. At 320px viewport: pricing grid is 1 column, no horizontal scroll
9. At 1280px viewport: pricing grid is multi-column (3-4 cards per row), no overflow
10. With OS reduced-motion enabled: hover transition is instant (base.css reduced-motion rule applies)
</verification>

<success_criteria>
- components.css: all pricing card styles, featured treatment, badge, btn-cta, floating CTA written with var() token references
- animations.css: ANIM-07 satisfied — `.pricing-card:hover { transform: var(--card-hover-lift) }` with CSS transition; no JavaScript required
- Featured cards: `border: var(--card-featured-border)` + `box-shadow: var(--card-featured-shadow)` — matches Phase 3 success criterion 2 exactly
- Non-featured cards: `border: var(--card-default-border)` — no glow
- Hover lift: `transform: translateY(-4px)` + box-shadow change via CSS :hover — no JS — matches Phase 3 success criterion 3
- Floating CTA: `position: fixed; bottom: var(--space-xl); right: var(--space-xl); z-index: var(--z-float); width: var(--cta-touch); height: var(--cta-touch)` — always accessible above all content
- Mobile (320px, 768px): single-column layout, no overflow (handled by layout.css plan 03-01; card widths in components.css must not use fixed widths that break this)
- Page is shippable as MVP after this plan — the full luxury gold-black pricing page is visually complete without any JavaScript animation
</success_criteria>

<output>
Create `.planning/phases/03-css-styling/03-02-SUMMARY.md` when done
</output>
